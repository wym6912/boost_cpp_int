# Boost class `cpp_int`

See `sample.cpp` for usage.

## Compile on Linux
```bash
g++ sample.cpp -I./include/ # include the boost directory
```

## Compile on Windows in Visual Studio 2022
Set `./include/` directory in `include directory`, or anywhere you like.
